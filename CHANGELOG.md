# CHANGELOG

## r114_3

- Select Object / View Result ウィンドウの表示倍率を設定する項目を追加。
- Hue, Wnd Scale に、説明用のツールチップを追加。

## r114_2

- OpenCV を更新(4.12.0 -> 4.13.0)。
- MinGW から、VS2022 でコンパイルするように変更。
- トラッカーの初期化に失敗したとき、クラッシュする問題を修正。
- 解析中、ログに解析フレーム位置を表示させていたが、削除。
- 高 DPI 環境において、レイアウトが崩れる問題を修正。
- Hue トラックバーが未初期化の数値を参照していた問題を修正。
- Hue トラックバーを操作すると、GDIハンドルが枯渇する問題を修正。
- 追加ダウンロードが必要なモデルが揃っていないとき、最初に見つからなかったモデルのパスのみを表示していたが、不足しているモデルをすべて探索したうえでまとめて表示するように変更。
- 表記の修正 (As Sub-filter/部分フィルター? -> As Sub-filter/部分フィルタ?)。
- 画面上部のメニューのボタンが大きすぎたので、縮小。
- 解析中に表示されるダイアログのウィンドウサイズの高さを縮小。
- 追加ダウンロードが必要なモデルが揃っていないとき、エラー画面が2回出ていたが、1回のみ表示するように変更。
- Insert Object をクリックしたときかつ解析結果が存在しないときに表示されるメッセージボックスのタイトルを`MotionTracking MK-II Plus for AviUtl2`から`Operation Error` に変更。

## r114_1

### AviUtl 版からの変更点

#### 主な変更点

- AviUtl の対応を終了。AviUtl ExEdit2 のみサポート。
- 32bit から 64bit に変更。
- 拡張子を `.auf` から `.aux2` に変更。
- Method の選択が、プルダウンメニューから選べるように変更。
- `Rect Hue` から `Hue` の表記に変更。
- AviUtl ExEdit2 のテーマに対応。
- Quick Blur, Easy Privacy を削除。
- ヘルパープラグインのPre-track: HSV Cvt、Pre-track: BGSubtraction については、今後のアップデートで対応予定。

#### Select Object

##### 追加機能

- ESC キーでキャンセルを追加。前回の選択状態があれば、それを復元して返すように変更。
- F3 キーで選択確定を追加。(x ボタンまで遠いため)
- Select Object を再度開いたとき、前回の選択矩形を表示した状態で開くように変更。

##### 変更点

- × ボタンでウィンドウを閉じたとき、結果を保存して返すように変更。
    - 旧版はキャンセルという概念がなく、閉じても状態がそのまま残るだけだった。

#### Analyze

##### 追加機能

- 解析中に進捗用ダイアログを表示するようにした。合わせて、解析中に x ボタンで解析をキャンセルできるようにした。
- 追加ダウンロードが必要な解析手法を選択し、そのモデルが存在しなかったとき、本来存在するべきモデルパスを表示するよう、ダイアログを表示するようにした。

##### 変更点

- 解析中に応答なしになることがあったので、修正。
- 解析ダイアログに表示される解析進捗(AviUtl 版で、ウィンドウタイトルに表示されていたもの)のフレームの番号の表示形式について、`解析位置のフレーム/解析終了フレーム`から、`解析中のフレーム番号/累計解析フレーム数`に変更。

#### View Result

##### 変更点

- チェックボックスにチェックを入れる方法から、結果確認用ウィンドウを表示して結果確認をするように変更。(AviUtl ExEdit2 において、チェックボックス式の対応が難しいため、従来の確認方法に戻した。)

#### Insert Object (旧 Save EXO)

##### 変更点

- 解析結果をそのまま Insert できるようにした。従来通りファイルに保存するには、画面上部の`File->Export Object File` を用いる。

#### Info

##### 追加機能

- ソフトウェア情報に関するダイアログを追加。
